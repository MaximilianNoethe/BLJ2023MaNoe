#include <stdio.h>
int main()
{
    // vorbereitung
    int x = 23;
    int y = 7;
    float p = 33.333;
    float q = 3.001;
    int a = 1;
    int b = 2;
    int c = 3;
    int d = b * b;
    int e = y + c;

    /*
    int lösung = c + (d * b) - e;
    printf("%d\n", lösung);

    float lösung2 = (float)x / (float)y;
    printf("%f\n", lösung2);

    float lösung3 = (float) p / q;
    printf("%f\n", lösung3);
    */

    /*
    b = ++a + a++;

    printf("%d\n", a);
    */
    /*
    int hex = 0xF, hex1 = 0x4, lösung;

    lösung = hex & hex1;

    printf("%d\n", lösung);


    lösung = hex | hex1;

    printf("%d\n", lösung);


   lösung = hex ^ hex1;
   printf("%d", lösung);
   */






    return 0;
}
